{
  "name": "ejam-test",
  "version": "1.0.0",
  "description": "ejam test assignment",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node server.ts",
    "build": "npm install --include=dev && tsc",
    "start": "node dist/server.js"
  },
  "author": "Mikita Rakovich",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.1",
    "@types/express": "^4.17.17",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
